---
title: "Bandit: Nivel 21 -> Nivel 22"
date: 2026-04-15 12:00:00 -0600
categories: [OverTheWire, Bandit]
tags: [linux, ctf, basico, cron, permisos]
---

## 🗓️ 15-04-2026 — Bandit Level 21

### 🔐 Credenciales

* **Usuario:** `bandit21`
* **Password:** `EeoULMCra2q0dSkYj561DX7s1CpBuOBt`
* **Host:** `bandit.labs.overthewire.org`
* **Puerto:** `2220`

---

### 🎯 Objetivo

Encontrar la contraseña del siguiente nivel.

Se nos indica que un programa se ejecuta automáticamente usando **cron**, por lo que debemos revisar:

```bash
/etc/cron.d/
```

---

### 🧰 Comandos utilizados

```bash
ls
cat
```

---

### 💻 Solución paso a paso

### 🔍 1. Revisar tareas programadas

```bash
ls /etc/cron.d
```

Encontramos:

```bash
cronjob_bandit22
```

---

### 📄 2. Ver el cronjob

```bash
cat /etc/cron.d/cronjob_bandit22
```

```bash
@reboot bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null
* * * * * bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null
```

👉 Esto significa:

* Se ejecuta al reiniciar
* Se ejecuta **cada minuto**
* Se ejecuta como el usuario `bandit22`

---

### 🧠 3. Analizar el script

```bash
cat /usr/bin/cronjob_bandit22.sh
```

```bash
#!/bin/bash
chmod 644 /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
cat /etc/bandit_pass/bandit22 > /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
```

---

### ⚙️ ¿Qué hace este script?

1. **Crea o sobrescribe un archivo en `/tmp`**
2. Escribe dentro la contraseña de `bandit22`
3. Le da permisos `644` → **lectura para todos**

---

### ⚠️ Detalle importante

La primera línea:

```bash
chmod 644 /tmp/archivo
```

👉 Puede fallar la primera vez porque el archivo aún no existe

Pero inmediatamente después:

```bash
cat /etc/bandit_pass/bandit22 > /tmp/archivo
```

👉 Lo crea automáticamente

---

### 🚀 4. Obtener la contraseña

Solo hay que esperar a que el cron se ejecute (máximo 1 minuto):

```bash
cat /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
```

Resultado:

```bash
tRae0UfB9v0UzbCdn9cY0gQnds9GF58Q
```

---

### 🧠 Explicación

Este nivel muestra un concepto clave:

👉 **Tareas automáticas pueden filtrar información sensible**

* El script corre como `bandit22`
* Tiene acceso a su contraseña
* La escribe en un archivo público (`/tmp`)
* Nosotros solo la leemos

---

### 💡 Tip

Siempre revisa:

* `/etc/cron.d/`
* `/var/spool/cron/`
* `/etc/crontab`

👉 Muchos fallos de seguridad vienen de **scripts automáticos mal configurados**

---

### 🚩 Flag

```bash
tRae0UfB9v0UzbCdn9cY0gQnds9GF58Q
```
